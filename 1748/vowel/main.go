package main

import "fmt"

func main() {
	halvesAreAlike("hello")
}

func halvesAreAlike(s string) {
	///	vowel := []string{"a", "e", "i", "o", "u", "A", "E", "I", "O", "U"}
	var str string
	//store := make(map[string]int)
	for i := 0; i < len(str); i++ {
		fmt.Println(str[100])
	}
}
