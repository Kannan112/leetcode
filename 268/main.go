package main

import (
	"fmt"
)

func main() {
	array := []int{4, 0, 1, 2}
	number := missingNumber(array)
	fmt.Println(number)
}
func missingNumber(nums []int) int {
	a := len(nums)
	ExpectedOutput:=a*(a+1)/2
	for 
}
